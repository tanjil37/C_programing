#include<stdio.h>
int main()
{
    int t,x,y,z,temp;
    scanf("%d", &t);
    for (int i = 0; i < t; i++)
    {
        /* code */
        scanf("%d %d %d", &x,&y,&z);

        if (x>y && x>z)
        {
            temp=x;
            
        }
        
    }
    
}