int main()
{
    char ch;
    scanf("%c", &ch);

    if(ch >= 'a' && ch <= 'z') {
        printf("%c Alphabet \n", ch);
    }
    if(ch >= 'A' && ch <= 'Z') {
        printf("%c Alphabet\n", ch);
    }
    else if("")
    return 0;
}
