#include <stdio.h>
int main()
{
    int num;
    scanf("%d", &num);
    if (num > 0)
        printf("The given numver is positive");
    else
        printf("The given numver is negetive");
    return 0;
}